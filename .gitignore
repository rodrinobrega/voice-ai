node_modules/
.env
.env.*
!.env.example
# No trailing slash: `dist/` matches directories only, so a `dist` *symlink*
# into build output would still show up as untracked and get committed.
dist
.output/
.nuxt/
coverage/
.serverless/
*.log
.DS_Store
.idea/
.vscode/
cypress/videos/
cypress/screenshots/
_to_delete/
*.zip
